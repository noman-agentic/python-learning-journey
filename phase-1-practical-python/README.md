# Phase 1 — Practical Python

## Topics Covered
- File handling (read/write, with statement)
- JSON and CSV processing
- Regular Expressions (regex)
- Modules and Packages
- Virtual Environments and pip
- Debugging and the logging module

## Mini-Project: Automation Data Cleaner

A small automation pipeline that reads messy signup data (CSV), validates and normalizes email and phone fields using regex, logs the entire process, and writes clean records to a JSON file.

### How to run
```bash
cd phase-1-practical-python/mini-project
python main.py
```

Output: `output/clean_data.json` (cleaned records), `automation.log` (process log, not committed).
